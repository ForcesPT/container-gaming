# Selkies listener health candidate

The 2026-09-24 OVH GRA11 L4 canary showed gateway `upstream_connect_refused` while the container was running, `pgrep -f selkies-gstreamer` still returned the same PID, and the Unix socket pathname still existed. The process-only health loop did not restart Selkies. The worker's socket-generation repin could not repair a listener that was not accepting on its existing socket.

This local candidate checks the container's `/proc/net/unix` table for the exact live listening socket, without opening an extra WebSocket connection. After the first healthy observation, two consecutive five-second checks that find no listener trigger the existing Selkies/compositor restart path. Initial startup and each restart get a 30-second bind grace period so a live Selkies process is not restarted while it is still opening signaling. An unreadable `/proc/net/unix` observation does not trigger a restart. The helper rejects symlinks, stale socket files, bound-but-not-listening sockets, and relative paths. Its Linux socket tests and `bash -n entrypoint.sh` pass.

This does not identify why Selkies stopped listening. The worker candidate from `ForcesPT/dpadplay` revision `718ce09` must also be redeployed so that it can repin the socket after this supervisor creates a replacement. The pinned OVH host launcher still offers the legacy stream overlay; the session controller strips it only for an exact allowlisted image digest. Any new image release must add its exact digest to that controller gate and its OVH cold-start selector, then verify that the baked entrypoint is the one Docker runs. No new image has been published and no second paid VM has been launched for this candidate.

The full source Dockerfile currently fails at the pinned Selkies v1.6.2 GitHub release asset (HTTP 404). `Dockerfile.listener-health` instead derives from the exact current production image `forcespt/dpadcloud-gaming@sha256:a6588db47431191308fe7a38c1b1c8854da04e0473fef5c203787d5188cdad4f` and replaces only the entrypoint and adds the helper. The pulled image's baked entrypoint SHA256 was `4ab21dcb3cee1f896c6527fbbb6e869f1db8bd5961c8ffde4d0a0f1975b17a41`, exactly matching the pinned source `e338bbb`. The built local derivative has image ID `sha256:78a07169018f50c8e5f16d62e0db3cffafc72102062f91a7d2c1e90a7be66778`. Before rollout, push a canary tag, verify its registry digest, and update both exact-digest gates if the registry digest differs. Then set the same immutable image reference for API and worker in a coordinated release.
